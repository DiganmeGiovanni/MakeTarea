{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Curso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Materia</th>
                <th>Nomprofesor</th>
                <th>Apeprofesor</th>
                <th>Emailprofesor</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('curso_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.materia }}</td>
                <td>{{ entity.nomProfesor }}</td>
                <td>{{ entity.apeProfesor }}</td>
                <td>{{ entity.emailProfesor }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('curso_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('curso_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('curso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
